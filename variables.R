#Variables in R language

#variables are used to store information

#variables contain latter,number,dot(.)and underscore character
# a variable name must start with a latter or a Dot but not start with underscore
#eg:
var_name<-67
var.name<-56

#but
_var_name<-67<--#not accepted
var_name*<-90<--#also not acceptable
  
#except dot and underscore no other special symbol is allowed in variable naming
.var_name<--78

4var_name<--89 #<--not allowed
#nmuber can come afterwords in variable but not in starting for example
var.name78<-67

#important notes--> Assignment of variables:
var<-45#--> very important used commonly
var1=78
67->var2
print(var2)
var3<-45

#cat function: it combines(hold) multiple values into print output.
cat(var1," ",var2,"/n",var3)

